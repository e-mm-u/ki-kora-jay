import React from 'react';
import './Toast.css'

const Toast = () => {
    return (
        <div>
           <button className='toast'>Completed the tasks</button> 
        </div>
    );
};

export default Toast;